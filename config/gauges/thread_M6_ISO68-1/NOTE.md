`GO.step` e `NOGO.step` non esistono ancora in questa cartella — vedi
`../README.md` e `../manifest.json` (`calibration_status: "not_modeled"`).

Vanno modellati con CAD convenzionale (M6, ISO 68-1, angolo profilo 60°),
non generati dall'IA. Questo file va rimosso quando i due STEP vengono
aggiunti.
